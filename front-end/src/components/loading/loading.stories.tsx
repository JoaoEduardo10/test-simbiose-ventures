import { Meta } from "@storybook/react";
import { Loading } from ".";

export default {
  title: "Loading",
  component: Loading,
} as Meta;

export const TamplateLoading = () => {
  return <Loading />;
};
