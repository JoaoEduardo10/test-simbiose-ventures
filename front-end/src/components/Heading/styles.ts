import { css, styled } from "styled-components";

export const HeadingConteiner = styled.h1`
  ${() => css`
    text-align: center;
    padding-top: 1rem;
    margin-bottom: 3rem;
    text-transform: uppercase;
  `}
`;
